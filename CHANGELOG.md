# Changelog

## 2.0.9 - 2026-09-18

### Windows 集成修复

- 修复从早期版本持续升级后，运行中的 MyTodo 仍可能显示 Electron 默认任务栏图标的问题；新版使用独立图标资源并刷新 Windows 图标缓存。
- 升级时重新写入仍然存在的桌面和开始菜单快捷方式图标与应用身份，避免继续沿用旧版快捷方式信息。
- 补全 Windows“已安装的应用”所需的卸载登记，增加安装位置、安装日期和完整卸载命令，安装完成后主动刷新系统应用列表。
- 调整安装位置页布局，使标题、中间安装信息和底部操作按钮使用统一的左右边界。

## 2.0.8 - 2026-09-18

### 安装与升级体验

- Windows 首次安装改为自包含的 HTML/WebView2 四步界面，统一欢迎、安装位置、实时进度和完成状态，不再显示安装用户范围选择。
- 手动运行安装包时会明确区分首次安装、版本升级、相同版本和降级；相同版本无需重复安装，降级安装保持禁止。
- 点击安装或升级后立即检查 MyTodo 是否仍在运行，未退出时停止安装；安装期间会阻止应用再次启动，并自动清理失效的安装锁。
- 应用内升级继续使用紧凑进度界面，保留下载百分比与速度；发现新版本时增加“稍后更新 / 立即更新”确认窗口。
- 首次安装与应用内升级窗口统一为主应用同款圆角、细灰边和 Windows 原生柔和阴影，并保持固定尺寸、不可缩放。
- 安装界面在页面完成绘制后再显示，避免 WebView2 初始化期间出现空白窗口；静默 NSIS 后端继续负责可靠写入和卸载。
- 安装期间持续阻止旧版 MyTodo 再次启动，修复启动锁提示窗口反过来占用文件并卡住升级的问题。
- 修复残留机器级安装记录可能导致升级时重复弹窗、错误提权或回到首次安装页的问题，统一按当前 Windows 用户完成安装和升级。

## 2.0.7 - 2026-09-17

### 应用内更新

- 应用内升级安装器改为紧凑的 MyTodo 专属界面，直接显示真实安装进度、当前阶段和完成状态。
- 原地升级继续沿用现有安装范围与目录，不再重复询问安装用户、安装位置和开机启动选项；完成后可直接启动新版。

### 多端同步基础

- 任务数据升级到 schema v3，为每条待办增加稳定 UUID、云端版本和本地同步状态，同时保留旧版数字 ID。
- 增加独立的持久化同步发件箱，支持原子写入、自动备份、损坏保护、未发送变更合并和重试状态记录。
- 新增系统加密的同步账户存储、短时访问令牌刷新、增量推送与拉取、后台退避重试和手动同步。
- 设置页增加多端同步状态和首次上传确认，现有本地待办不会在用户确认前上传；首次注册后只显示一次恢复密钥。
- 网络或服务端不可用时继续保留本地编辑和提醒，并在连接恢复后发送持久化队列。
- 数据目录迁移现在会携带同步发件箱、加密账户及自动备份；导出备份不包含账户密钥。

## 2.0.6 - 2026-09-16

### 提醒体验

- 将任务提醒窗无操作时的默认倒计时由 5 秒延长至 15 秒，给用户留出更充足的阅读和操作时间。
- 新增提醒弹窗位置设置，可在右下角模式与更宽、更紧凑的顶部居中模式之间切换；两种模式均使用不透明信息面板，切换时正在显示的提醒会立即重新布局和定位。

## 2.0.5 - 2026-09-16

### 更新与退出稳定性

- 修复应用内升级时主窗口关闭后，隐藏提醒窗口仍维持后台进程并阻止安装覆盖的问题。
- 自动更新、托盘退出和关闭确认现在共用完整退出流程，统一销毁全部窗口和托盘，并在异常时强制释放进程与单实例锁。
- 应用内升级会真正跳过自定义安装目录页，只显示安装进度和完成页。
- 恢复更新下载期间的百分比、实时速度和进度条；非下载状态不显示额外提示文字。

## 2.0.4 - 2026-09-16

### 更新体验

- Windows 安装版点击“下载并安装”后，下载完成会直接启动原地升级安装器，不再要求再次点击重启安装。
- 应用内升级会沿用既有安装目录，跳过版本确认、安装目录和开机启动选择，仅显示安装进度与完成页。

### 提醒稳定性

- 修复多条提醒连续出现或延后提醒再次出现时，提醒窗按钮可能一直不可用、窗口无法自动关闭的问题。
- 提醒队列切换时不再反复隐藏和显示原生窗口，最后一条提醒处理完毕后会确认窗口已经隐藏。
- 提醒操作等待主进程响应超时后会自动恢复按钮，避免偶发通信异常导致窗口永久卡住。

## 2.0.2 - 2026-09-15

### 修复与发布改进

- 修复 Windows 已安装版的任务栏图标：运行时使用安装目录 `resources` 下的实体图标文件。
- 安装器发现 MyTodo 正在运行时会提示先从托盘退出，并立即结束安装，不再继续覆盖运行中的程序。
- GitHub Release 同时提供安装包、`latest.yml` 和 `.blockmap`，恢复 Windows 安装版的检查、下载与重启安装更新流程。
- 发布成品核验改为验证实体运行时图标，避免将任务栏图标错误地依赖于 ASAR 内资源。

## 2.0.1 - 2026-09-15

### 修复与改进

- 提醒窗的延后菜单增加“本次不再提醒”，可跳过当前提醒而不关闭该任务后续的提醒。
- 改进托盘退出流程：退出后立即移除托盘图标；点击托盘图标重新打开时会恢复并聚焦主窗口。
- 安装器将开机启动选项改为更直白的“开机自动启动 MyTodo”。

## 2.0.0 - 发布候选

开发日期：2026-09-14。正式发布日期以 Release 页面为准。

### 新功能

- 重构主界面为待办、日历和设置三个职责清晰的工作区。
- 首页新增已逾期、今天、未来 7 天、全部和已归档视图；未来 7 天按今天、明天及具体日期分组，并支持搜索、排序和快捷完成。
- 恢复长列表回到顶部按钮，新建任务支持复制最近一次创建的完整内容。
- 合并普通任务、循环任务的新增与编辑窗口，实时展示提醒节点。
- 高、中、低优先级分别默认使用强提醒、标准提醒和轻提醒，并允许单任务覆盖或自定义节点。
- 增加每日摘要、每周概览和安静时段设置。
- 提醒窗增加完成任务、延后 15 分钟、延后 1 小时和延后到次日 09:00，并在 5 秒无操作后默认延后 15 分钟。
- 提醒窗改用 Soft Bell Ding 提示音；每日和每周摘要的任务内容按行展示，提醒面板使用不透明底色与 Electron 原生窗口阴影。
- 日历改为只展示时间分布和当日日程，点击任务统一进入详情。
- 日历每日任务列表增加固定占位的提醒铃铛，长标题不会覆盖图标。
- 任务详情增加带二次确认的删除入口，日历中的任务也可直接删除。
- Windows 安装版直接读取 GitHub 最新正式 Release 资源，增加自动检查、手动下载进度和确认后重启安装。
- Windows 安装器增加已安装版本提示、同版本重装确认、升级确认和降级拦截。
- 自定义安装目录会自动补齐 `MyTodo` 末级目录，并可在安装时选择后台开机启动。
- 设置页增加数据目录、JSON 备份导出和真实提醒服务状态；运行日志保留在底层用于故障排查。

### 行为与数据

- 任务数据升级到 schema v2，新增提醒模式、自定义节点和多节点发送历史，并兼容旧版数据。
- 应用错过多个提醒节点后只发送最近的有效节点，避免重启时连续弹出历史提醒。
- 提醒服务每 30 秒扫描未来 60 秒，并由每秒触发队列校准临近提醒，重叠窗口避免跨轮询漏报。
- 一次性任务逾期后按自然日最多提醒一次，并支持延后提醒。
- 每日循环和每周循环自动裁剪会与下次发生重叠的提醒节点。
- 日历切换日期时复用内存数据，不重复读取任务文件。
- 提醒窗完成任务后即时刷新主界面。
- 应用只允许一个实例运行，重复启动会恢复已有窗口。
- 任务主文件与备份同时损坏时进入只读保护，并保留异常文件供用户恢复。
- JSON 文件增加结构校验，恢复时不会用错误结构覆盖健康备份。
- `--user-data-dir` 现在会隔离数据位置记录，不再意外读取正式环境的自定义目录。
- 首次关闭主窗口时提示应用仍在托盘检查提醒。
- 默认窗口调整为 1100×760，最小窗口调整为 860×680。

### 界面

- 采用更紧凑的工作台布局，统一控件、边框、圆角与阴影。
- 弹窗恢复 1.0 的一体式灰白玻璃布局，标题、内容和操作区不再呈现分块感。
- 保留自绘最小化、最大化/还原和关闭按钮。
- 统一任务详情标签对齐及长内容滚动行为。
- 单词悬浮入口在 2.0 中继续隐藏。
- 开发版首页提供测试提醒按钮，安装版中隐藏并由主进程拒绝调用。

### 已知限制

- 当前只完成 Windows x64 发布验证计划。
- 应用完全退出后无法继续调度提醒。
- 安装包尚未进行商业代码签名。
- 自动更新仅支持 Windows NSIS 安装版，首次跨版本升级仍需人工验收。
- 最终安装版版本判断、开机自启、任务栏图标、多显示器缩放与长时间运行仍需人工验收。

## 1.0.0 - 发布候选

- 实现普通待办、循环任务、日历、归档、托盘和应用内提醒。
- 完成主进程、存储、窗口、IPC 和渲染资源的初步模块化。
- 增加原子写入、备份恢复、安装检查、发布校验和 Windows 多尺寸图标。
- 单词悬浮功能实现保留，界面入口暂时隐藏。
