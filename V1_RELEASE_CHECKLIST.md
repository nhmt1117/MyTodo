# MyTodo v1 发布清单

更新日期：2026-09-03。当前为 v1.0.0 发布候选；核心功能、打包、安装和依赖安全审计已完成，仍有少量真实设备验收与对外发布动作待办。

## 已完成

- [x] 主进程、数据存储、窗口管理、IPC、提醒规则及渲染资源模块化。
- [x] 删除正式界面的测试按钮及测试 IPC，而非只做样式隐藏。
- [x] 实现普通待办通知调度、每日/每周/每月循环规则和提醒去重。
- [x] 修复日历在 29–31 日跨月时可能跳过月份的问题，并覆盖闰年和跨年测试。
- [x] JSON 原子替换、备份恢复、失败写入保留原文件及测试。
- [x] 悬浮窗位置持久化、无四向拖动光标、双击正文隐藏及按钮区域排除。
- [x] 新增删除确认弹窗；已在安装版隔离数据中验证取消保留任务、确认后删除任务。
- [x] 统一名称和 v1.0.0 元数据，填写 author、appId、MIT LICENSE，并补齐 README、变更说明、测试记录和发布清单。
- [x] 修复批处理调用、错误码、工作目录及 CRLF 换行问题。
- [x] 升级至 Electron 44.1.1、electron-builder 26.15.3，并加入 Electron 内置 ZIP 解压大文件回归测试。
- [x] 使用 npm 官方 registry 执行 `npm audit`，结果为 0 vulnerabilities。
- [x] 自动化检查 26 项通过，包含数据读写、循环规则、提醒重试、发布完整性、图标结构及删除确认入口。
- [x] 构建 Windows x64 NSIS 安装包，写入 MyTodo 图标和 1.0.0 版本信息，并验证 ASAR 源码/文档比对和 SHA-256 生成。
- [x] 实测安装向导、卸载、再次安装，以及桌面和开始菜单快捷方式启动已安装的 MyTodo。
- [x] 通知任务在安装版隔离数据中两次触发 Electron `show` 回调并写入去重状态。

## 发布前仍需完成

- [ ] 在本机肉眼确认 Windows toast 的视觉展示，并点击一次，确认它会恢复主窗口。自动化接口无法枚举 Windows toast 为可操作窗口。
- [ ] 验证 Windows 任务栏图标；安装器、安装后的程序和两个快捷方式的图标资源已确认，但自动化截图无法覆盖任务栏区域。
- [ ] 在安装版中关闭并重启应用，确认悬浮窗恢复保存位置。
- [ ] 确认开机自启开关及登录后的实际启动行为。
- [ ] 长时间保持悬浮窗打开，确认任务栏不出现额外 MyTodo 窗口；补测显示器拔插/缩放。此两项按你的安排延期。
- [ ] 初始化本地 Git、首次提交并推送到 `https://github.com/nhmt1117/MyTodo.git`，创建 `v1.0.0` 标签和发布页、上传安装包与 `SHA256SUMS.txt`。远端当前未返回任何引用，且尚未执行推送。

## 可延后到 v1.1

- 将主窗口 UI 进一步组件化，减少 `renderer/renderer.js` 的函数堆叠。
- 增加导入/导出待办数据能力。
- 增加提醒音、通知免打扰、提醒提前量等设置。
- 增加搜索、筛选、今日视图。
- 增加自动更新或版本检查。

详细证据及限制见 [发布测试记录](V1_RELEASE_TEST_REPORT.md)。
