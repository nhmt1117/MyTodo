# MyTodo v2.0.9 发布清单

更新日期：2026-09-18。此清单将自动检查与真实安装环境验收分开记录；只有全部必要项完成后才建议创建 `v2.0.9` 标签。

## 代码与自动检查

- [x] 版本号更新为 2.0.9，保留稳定的 appId 和产品名称。
- [x] 首页、日历、设置与统一任务编辑窗口按 2.0 信息架构实现。
- [x] 普通任务和循环任务共用一套新增/编辑流程。
- [x] 日历点击复用内存任务数据，不因重复读取文件出现闪动。
- [x] 日历任务详情支持删除，并复用统一的二次确认与数据刷新流程。
- [x] 任务数据 schema v3 兼容旧版字段，并持久化稳定 UUID、同步状态、提醒模式、节点和发送记录。
- [x] 多端同步本地基础支持加密账户、持久化发件箱、首次上传确认、增量推拉和断网重试。
- [x] 优先级自动提醒、单任务覆盖、自定义节点及循环任务节点裁剪。
- [x] 每日摘要、每周概览、安静时段及配置持久化。
- [x] 自定义提醒窗支持完成、打开详情和三种延后方式。
- [x] 提醒窗 15 秒无操作后默认延后 15 分钟，按钮同步显示剩余秒数。
- [x] 提醒服务每 30 秒扫描未来 60 秒，临近提醒进入每秒校准队列，扫描窗口保持重叠。
- [x] 一次性任务逾期后每天最多提醒一次，并支持延后、安静时段和单任务关闭提醒。
- [x] 未来 7 天视图按今天、明天及后续具体日期逐日分组。
- [x] 新建任务支持复制最近一次创建内容，长列表恢复回到顶部按钮。
- [x] 日历每日任务列表固定预留提醒铃铛位置，并对长标题使用省略显示。
- [x] 开发版提供测试提醒按钮，安装版隐藏入口并拒绝主进程调用。
- [x] 空标题内联校验、80/500 字限制、删除确认和详情长内容布局。
- [x] 数据目录查看、选择与旧数据迁移流程保留。
- [x] 单实例保护阻止重复窗口、重复提醒和并发数据写入；第二次启动会恢复原窗口。
- [x] JSON 结构校验、双重损坏只读保护和启动异常提示不会覆盖原文件。
- [x] 设置页支持打开数据目录和导出完整 JSON 备份；本地运行日志功能保留但不向普通用户单独展示入口。
- [x] 提醒服务状态来自真实调度器检查结果，不再固定显示“正常”。
- [x] 第一次关闭主窗口时提示应用仍在托盘运行，之后不重复打扰。
- [x] 单词悬浮入口继续从正式界面移除。
- [x] 隔离 Electron 验收覆盖默认尺寸、最小尺寸、日历、统一编辑窗、设置、任务提醒和摘要提醒，控制台 0 错误。
- [x] `npm run check` 全部通过（86 项测试，0 失败）。
- [x] Windows NSIS 安装版已接入 GitHub 最新正式 Release 资源、下载进度和用户确认安装。
- [x] 安装器显示当前与目标版本，相同版本无需重复安装，升级需要确认，并阻止降级安装。
- [x] 自定义安装父目录会补齐 `MyTodo` 末级目录，安装选项可同步后台开机启动设置。
- [x] 应用内更新使用紧凑专属界面展示真实安装进度，沿用现有安装范围与目录，完成后直接启动新版。
- [x] 与 `package.json` 一致的版本标签（例如 `v2.0.9`）会触发 Windows 工作流，创建包含安装包、更新元数据和中文说明的正式 Release。
- [x] `npm run build:win` 成功生成 Windows 安装包、`.blockmap` 和 `latest.yml`。
- [x] `npm run verify:release` 完成 39 个打包文件比对并生成 SHA-256。

## 开发版人工流程

- [ ] 新建普通任务，分别测试低、中、高优先级的自动提醒预览。
- [ ] 将任务切换为循环任务，核对每日、每周、每月在日历中的日期分布。
- [ ] 编辑任务后确认首页、日历、详情和提醒计划同步更新。
- [ ] 测试今天、未来 7 天、全部、已归档、搜索及三种排序。
- [ ] 分别从待办页和日历详情测试删除取消与确认，并测试完成、恢复和关闭单任务提醒。
- [ ] 空标题保存后仍可直接输入；80 字标题与 500 字备注不挤破窗口。
- [ ] 连续点击日历日期和跨月按钮，确认无闪白、卡死或日期跳月。
- [ ] 在最小窗口 860×680 和最大化状态检查文字、菜单、弹窗与遮罩边缘。
- [ ] 修改每周起始日、摘要时间、安静时段，重启后确认设置保留。
- [ ] 更换数据目录，确认旧任务和设置被迁移且重启后读取新目录。
- [ ] 连续启动两次应用，确认只保留一个主窗口和一个托盘图标。
- [ ] 打开数据目录，确认其中的运行日志可用于排查问题；导出备份并确认生成的 JSON 文件可正常读取。
- [ ] 首次关闭主窗口显示托盘提示，再次关闭不重复提示。

## 提醒实测

- [ ] 创建 2 分钟内到期的自定义节点任务，确认提醒窗在目标秒附近出现、置顶且不进入任务栏。
- [ ] 保留一个未完成任务跨过截止日期，确认之后每天最多出现一次逾期提醒。
- [ ] 提醒窗保持无操作，确认显示 15 秒倒计时，结束后窗口关闭并将任务延后 15 分钟。
- [ ] 点击提醒窗非按钮位置，确认主窗口恢复并直接打开对应任务详情。
- [ ] 点击“完成任务”，确认任务归档且已打开的主界面即时刷新。
- [ ] 分别验证延后 15 分钟、1 小时和次日 09:00；重启应用后延后状态仍有效。
- [ ] 制造多条同时提醒，确认队列逐条显示且不重复。
- [ ] 验证每日摘要和每周概览只在对应周期显示一次。
- [ ] 验证安静时段内不弹出，离开安静时段后只补最近的有效节点。
- [ ] 长时间保持托盘运行，确认不额外占用任务栏且没有重复提醒。

## 安装版与发布

- [ ] 从 1.x 安装版升级到 2.0.9，确认原任务、数据目录和窗口设置可继续使用。
- [ ] 已安装 2.0.9 时再次运行安装包，确认显示当前版本并提示无需重新安装。
- [x] 以 2.0.8 已安装状态运行 2.0.9，确认应用内精简升级完成且卸载登记、快捷方式和图标资源均已更新。
- [ ] 以更高已安装版本运行 2.0.9，确认降级仍被阻止。
- [x] 保持 MyTodo 正在运行后开始升级，确认安装器在写入文件前停止；安装期间再次启动旧版应用不会卡住升级。
- [x] 验证 `D:\Software`、已以 `MyTodo` 结尾的目录和磁盘根目录，最终只追加一级正确的 `MyTodo`。
- [ ] 验证安装、启动、关闭到托盘、托盘恢复、退出和卸载。
- [x] 验证升级后的任务栏、桌面快捷方式和开始菜单均使用 MyTodo 图标；托盘继续使用同一图标源。
- [ ] 分别取消和勾选安装器的开机启动选项，确认应用设置同步，并验证登录后只在托盘后台运行。
- [ ] 在 100%、125%、150% 缩放及可用的多显示器环境检查窗口和提醒位置。
- [ ] 在干净 Windows 用户环境复验，无 Node.js 也可安装运行。
- [ ] 在安装版“设置 → 关于”中验证手动检查、无更新、下载进度和更新错误状态。
- [ ] 从已安装的 2.0.7 完成一次 GitHub Release 真实跨版本下载和安装。
- [ ] 将安装包、`.blockmap` 和 `latest.yml` 上传到 GitHub Release；普通用户手动安装时只下载 `.exe`。
- [ ] 发布说明包含功能变化、升级兼容、已知限制和未签名提示。
- [ ] 最终确认后创建并推送 `v2.0.9` 标签。

## 可延后事项

- 商业代码签名。
- macOS/Linux 安装包适配和验收。
- 单词悬浮功能重新设计与重新开放。
- 数据备份导入、恢复和跨设备同步服务端正式上线。
