# MyTodo v2.0.10 发布测试记录

测试版本：2.0.10 发布候选

测试日期：2026-09-18

测试平台：Windows 11 x64，Node.js 24，npm 11，Electron 44.1.1，electron-builder 26.15.3。

## 当前状态

2.0 的代码重构、提醒模型、Windows 自动更新、自动化检查和 Windows 安装包构建均已完成。2.0.10 增加完整数据备份恢复与恢复前快照、系统休眠/解锁/时间跳变后的提醒队列重建，以及主界面和提醒窗渲染异常自恢复。本机已完成 2.0.9 到 2.0.10 的文件级真实升级；可见安装界面、真实休眠提醒和更多 Windows 环境仍需按 [发布清单](V2_RELEASE_CHECKLIST.md) 人工走查。

## 自动化范围

- JavaScript 语法与正式资源引用。
- 一次性任务及每日、每周、每月循环日期。
- 优先级提醒节点、单任务自定义、错过节点补发、发送去重、每日逾期提醒，以及 30 秒前瞻扫描与每秒精确触发队列。
- 延后提醒持久化、摘要周期去重和安静时段。
- 任务 schema v3、稳定 UUID、同步发件箱、旧数据迁移、原子写入、结构校验、备份恢复和只读保护。
- 同步账户系统加密、首次上传确认、增量推拉、断网持久化及后台退避重试。
- 单实例恢复、数据导出与校验恢复、恢复前安全快照、启动异常提示、本地日志保留和提醒服务状态。
- 系统休眠恢复、屏幕解锁、系统时间或时区跳变后的提醒队列重建。
- 主界面和提醒窗渲染进程崩溃或持续无响应后的自动重载；提醒窗恢复时保留当前提醒。
- `--user-data-dir` 使用独立的数据位置记录，不读取正式环境的自定义目录。
- 日历无异步重复读取、统一编辑窗、复制上次创建、日历详情删除确认、提醒铃铛占位、未来 7 天逐日分组、回到顶部和正式界面入口检查。
- Windows 批处理、Electron 安装完整性、发布元数据和 ICO 多尺寸结构。
- Windows 安装版更新状态机、便携版隔离、用户确认下载与安装、GitHub 最新正式 Release 资源和草稿发布流程。
- HTML 安装器的版本识别、升级确认、同版本与降级拦截、目录末级规范化、静默 NSIS 后端和开机启动选项同步。

最近一次 `npm run check`：97 项测试通过，0 失败，0 跳过。

## Electron 界面验收

已在隔离数据目录中完成以下检查，所有页面均无控制台错误或视口溢出：

- 1100×760 默认窗口和 860×680 最小窗口。
- 首页任务分组、搜索、筛选、排序和菜单。
- 日历 42 个日期格、跨月、选中状态和右侧日程。
- 统一任务编辑、提醒模式切换、实时节点预览和空标题提示。
- 设置四个分区及持久化字段。
- 数据工具在 1100×760 和 860×680 窗口中无横向溢出或控件重叠。
- 使用独立 `--user-data-dir` 实际连续启动两次开发版，隐藏的原窗口会恢复可见，始终只保留一个主窗口与一个提醒窗口，运行日志无错误。
- 首次关闭主窗口后，托盘提示状态正确写入隔离配置；隔离数据路径未读取正式环境的自定义目录。
- 任务提醒、摘要提醒、完成、打开详情和延后菜单。
- 圆角、遮罩、窗口控制、文本截断和无重叠。
- 隔离 Chromium 检查确认复制上次任务成功，日历铃铛槽数量正确，长标题省略且回到顶部按钮可见。
- 使用打包后的 2.0.10 和隔离数据目录实际启动，连续启动两次始终只保留一个主进程。
- 实际结束主界面和提醒窗的两个渲染进程后，两者均自动重建，主进程保持运行且日志记录对应恢复原因。
- 将隔离实例配置为“关闭即退出”后发送正常关闭消息，主进程及全部子进程均退出，没有后台残留。

## 构建产物

- `npm run build:win` 已成功生成自包含的 `dist/MyTodo-Setup-2.0.10.exe`、对应 `.blockmap` 和 `latest.yml`。
- `npm run verify:release` 已完成 39 个打包源码文件比对，并验证更新元数据、实体运行时图标、安装器载荷尾标和 SHA-256。
- 打包后的更新源已指向 GitHub 最新正式 Release 下载资源；2.0.10 工作流会同时上传安装包、`.blockmap` 和 `latest.yml`。
- 2.0.10 安装目录页已收窄中间信息区右边界，安装完成和更新完成信息位置按确认稿下移。
- 本机存在“Windows 卸载登记指向旧 C 盘副本、桌面快捷方式和正在运行程序指向 D 盘副本”的历史双目录状态；实测发现并修复了外壳和 NSIS 后端两层目录覆盖问题。
- 从最终安装包提取的同一后端已将 `D:\Software\MyTodo` 从 2.0.9 升级至 2.0.10，退出码为 0；六个正式数据文件的 SHA-256 全部保持不变。
- 升级后卸载登记、桌面快捷方式和开始菜单快捷方式均统一指向 `D:\Software\MyTodo`，旧 C 盘程序副本已清理，新版启动日志无错误。
- 2.0.9 已实际验证过可见的欢迎、目录、升级、运行中拦截、安装进度、完成、同版本和降级流程；本轮 Windows 图形控制组件不可用，因此 2.0.10 的可见安装界面仍列为人工确认项。

## 必须人工确认

自动化无法代替以下真实系统行为：

- 卸载、无开发环境的全新 Windows 用户安装，以及缺失 WebView2 Runtime 时的提示。
- Windows 任务栏、托盘和快捷方式图标。
- 开机自启登录流程。
- 多显示器和不同 DPI 缩放。
- 长时间托盘运行及真实时间提醒。
- 未签名安装包在 SmartScreen 中的实际提示。
- 正式发布后从 2.0.9 通过 GitHub Release 检查、下载、安装和数据保留。
- 曾安装早期版本的电脑可能保留 Windows 应用清单或固定任务栏图标缓存；若升级后未立即刷新，需要重新登录/重启，或取消固定后重新固定。

## 已知限制

- 应用完全退出后不会作为系统服务自行唤醒。
- 安静时段结束后的补发依赖应用仍在运行。
- 当前发布目标为 Windows x64，安装包未进行商业代码签名。
- 自动更新仅在附带更新元数据的 Windows NSIS 安装版中启用；开发模式、便携版和非 Windows 平台不参与更新。
- 单词悬浮功能在 2.0 正式导航中保持隐藏。
