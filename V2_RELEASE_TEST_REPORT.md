# MyTodo v2.0.0 发布测试记录

测试版本：2.0.0 发布候选

测试日期：2026-09-15

测试平台：Windows 11 x64，Node.js 24，npm 11，Electron 44.1.1，electron-builder 26.15.3。

## 当前状态

2.0 的代码重构、提醒模型、Windows 自动更新、自动化检查、隔离 Electron 界面验收和 Windows 安装包构建均已完成。真实安装、跨版本升级、系统集成和真实时间提醒仍需按 [发布清单](V2_RELEASE_CHECKLIST.md) 人工走查。

## 自动化范围

- JavaScript 语法与正式资源引用。
- 一次性任务及每日、每周、每月循环日期。
- 优先级提醒节点、单任务自定义、错过节点补发、发送去重、每日逾期提醒，以及 30 秒前瞻扫描与每秒精确触发队列。
- 延后提醒持久化、摘要周期去重和安静时段。
- 任务 schema v2、旧数据迁移、原子写入、结构校验、备份恢复和只读保护。
- 单实例恢复、数据导出备份、启动异常提示、本地日志保留和提醒服务状态。
- `--user-data-dir` 使用独立的数据位置记录，不读取正式环境的自定义目录。
- 日历无异步重复读取、统一编辑窗、复制上次创建、日历详情删除确认、提醒铃铛占位、未来 7 天逐日分组、回到顶部和正式界面入口检查。
- Windows 批处理、Electron 安装完整性、发布元数据和 ICO 多尺寸结构。
- Windows 安装版更新状态机、便携版隔离、用户确认下载与安装、GitHub 最新正式 Release 资源和草稿发布流程。
- NSIS 已安装版本读取、同版本重装确认、升级确认、降级拦截、目录末级规范化和开机启动选项同步。

最近一次 `npm run check`：67 项测试通过，0 失败，0 跳过。

## Electron 界面验收

已在隔离数据目录中完成以下检查，所有页面均无控制台错误或视口溢出：

- 1100×760 默认窗口和 860×680 最小窗口。
- 首页任务分组、搜索、筛选、排序和菜单。
- 日历 42 个日期格、跨月、选中状态和右侧日程。
- 统一任务编辑、提醒模式切换、实时节点预览和空标题提示。
- 设置四个分区及持久化字段。
- 数据工具在 1100×760 和 860×680 窗口中无横向溢出或控件重叠。
- 使用独立 `--user-data-dir` 实际连续启动两次开发版，隐藏的原窗口会恢复可见，始终只保留一个主窗口与一个提醒窗口，运行日志无错误。
- 首次关闭主窗口后，托盘提示状态正确写入隔离配置；隔离数据路径未读取正式环境的自定义目录。
- 任务提醒、摘要提醒、完成、打开详情和延后菜单。
- 圆角、遮罩、窗口控制、文本截断和无重叠。
- 隔离 Chromium 检查确认复制上次任务成功，日历铃铛槽数量正确，长标题省略且回到顶部按钮可见。

## 构建产物

- `npm run build:win` 已成功生成 `dist/MyTodo-Setup-2.0.0.exe`、对应 `.blockmap` 和 `latest.yml`。
- `npm run verify:release` 已完成 36 个打包源码文件比对，并验证更新元数据与安装包结构。
- 打包后的更新源已指向 GitHub 最新正式 Release 下载资源；但 2.0.0 Release 按发布策略只附加安装包，未上传 `latest.yml`，应用内自动更新暂不可用。
- 安装包尚未在本轮执行安装、升级和卸载，因此这些项目仍保留为人工待确认。
- 更新状态与下载流程已自动化模拟；由于尚无公开的更高版本 Release，真实跨版本更新仍待用 2.0.1 验收。
## 必须人工确认

自动化无法代替以下真实系统行为：

- NSIS 安装、升级、卸载和无 Node.js 环境启动。
- Windows 任务栏、托盘和快捷方式图标。
- 开机自启登录流程。
- 多显示器和不同 DPI 缩放。
- 长时间托盘运行及真实时间提醒。
- 未签名安装包在 SmartScreen 中的实际提示。
- 2.0.0 到 2.0.1 的 GitHub Release 检查、下载、重启安装和数据保留。

## 已知限制

- 应用完全退出后不会作为系统服务自行唤醒。
- 安静时段结束后的补发依赖应用仍在运行。
- 当前发布目标为 Windows x64，安装包未进行商业代码签名。
- 自动更新仅在附带更新元数据的 Windows NSIS 安装版中启用；开发模式、便携版和当前仅含安装包的 2.0.0 Release 不参与更新。
- 单词悬浮功能在 2.0 正式导航中保持隐藏。
